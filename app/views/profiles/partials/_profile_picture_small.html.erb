<%= link_to profile_path(@story.user) do %>
  <% if @story.user.user_image.attached? %>
    <%= image_tag url_for( @story.user.user_image), alt: "Image", class: "rounded-circle profile-img-md" %>
  <% else %>
    <%= image_tag "user.png", style: "width:50px"%>
  <% end %>
<% end %>