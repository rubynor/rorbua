
<nav class="navbar sticky-top navbar-light shadow-sm" style="background-color: #2B7A78; z-index: 2;">
  <div class="d-flex justify-content-center align-items-center">
    <a href="#sidebar" data-bs-toggle="offcanvas" role="button" arial-controls="sidebar" class="p-2 m-2 d-none d-lg-block">
      <span class="material-icons" style="color: white">menu</span>
    </a>
    <h3 style="font-family: 'Dancing Script', serif" class="px-2"><%= link_to "Storytime", root_path, class:"logo"%></h3>
  </div>
  <div>
    <ul class="nav">
     <!-- render 'layouts/language_selector' -->
      <% if user_signed_in? %>
        <li class="nav-item dropdown">
          <a data-bs-toggle="dropdown" href="#" role="button" aria-expanded="false">

            <% if current_user.user_image.attached? %>
              <%= image_tag url_for(current_user.user_image), alt: "Image", class: "profile-img profile-img-sm" %>
            <% else %>
              <span class="material-icons" style="color:white">account_circle</span>
            <% end %>

          </a>
          <ul class="dropdown-menu dropdown-menu-end settings-tab">
            <li><%= link_to "Min konto",  profile_path(current_user), class: "dropdown-item"%></li>
            <li class="dropdown-item">
                <%= button_to t(".log_out"), destroy_user_session_path, method: :delete %>
            </li>
          </ul>
        </li>
      <% else %>
        <%= link_to t(".log_in"), new_user_session_path, class: "nav-item btn btn-info" %>
      <% end %>
    </ul>
  </div>
</nav>

