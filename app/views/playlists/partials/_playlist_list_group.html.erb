<%= button_to playlist_stories_path(story_id: $story, playlist_id: playlist), hidden:true do%>
  <button class="list-group-item list-group-item-action" data-bs-dismiss="modal" type="submit"><%= playlist.title %></button>
<% end %>