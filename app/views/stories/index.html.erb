<%= render "stories/partials/category_filter"%>

<div id="stories" class="feed_stories_container mt-1">
  <div class="container">
    <button type="button" class="btn btn-secondary mt-1 " data-bs-toggle="modal" data-bs-target="#category-modal" aria-expanded="false" margin-top="1">Søk i stories</button>

    <div class="row g-4">

      <% @stories.each do |story| %>
        <div id="<%= dom_id story %>" class="col-xl-3 col-lg-4 col-md-6 mb-2">
          <%= render "stories/partials/story_card", story: story %>
        </div>
      <% end %>
    </div>
  </div>
</div>
